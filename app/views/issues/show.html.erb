<% if old_browser?(@user_agent) %>
  <% form_class = "" %>
<% else %>
  <% form_class = "dropzone" %>
  <%= javascript_tag do %>
    <%= render :partial => 'dropzone', :handlers => [:erb], :formats => [:js] %>
    <%= render :partial => "issue_assign", :handlers => [:erb], :formats => [:js] %>
  <% end %>
<% end %>

<% content_for :sidebar do %>
  <div class="row-fluid">
    <%= link_to project_issues_path(@issue.project), class: 'btn span24 box' do %>
      <i class="icon-arrow-left"></i><%= @issue.project.name %>
    <% end -%>

    <div class="issue-info">
      <div class="attributes">
        <%= form_tag (@issue.closed? ? reopen_issue_path(@issue) : close_issue_path(@issue))  do %>
          <div id="status-switch" class="switch" data-on-label="<i class='icon-open'></i> 开放" data-off-label="<i class='icon-closed'></i> 关闭">
            <input type="checkbox" <%= 'checked' if !@issue.closed?  %> />
          </div>
        <% end %>
      </div>
    </div>

    <% if User.current.allowed_to?(:add_issue_watchers, @project) ||
      (@issue.watchers.present? && User.current.allowed_to?(:view_issue_watchers, @project)) %>
      <div id="watchers">
        <%= render :partial => 'watchers/watchers', :locals => {:watched => @issue} %>
      </div>
    <% end %>
  </div>
<% end -%>

<div class="issue">
  
  <div class="issue-header clearfix">
    <h2>
      <%= @issue.subject %>      
    </h2>
    <div id="attributes" class="attributes">          
      <div class="span7 holder">
        <span><%= l(:field_assigned_to) %>：</span>
        <span  id="status" class=""><strong><%= @issue.assigned_to.try(:name) %></strong></span>
        <% if !@issue.closed? %>
          <a href="#" id="pencil" class="popover-link"><i class="icon-pencil"></i></a>               
        <% end %>  
      </div>

      <div class="span8  holder">
        <span><%= l(:field_due_date) %>：</span>
        <input type="text" id="datepicker" readonly="readonly" value="<%= format_date(@issue.due_date) %>" class="<%= 'overdue' if @issue.overdue? %>" />
      </div>
    </div>
    <div id="edit-btn">
      <%= render 'action_menu', { issue: @issue } %>
    </div>      
  </div>

  <div class="row journal">
    <div class="avatar span2">
      <%= link_to_user(@issue.author, :avatar => :small) %>
    </div>    
    <div class="issue-holder span14">
      <div class="author-header clearfix">
        <div class="author">
          <strong><%= link_to_user(@issue.author) %></strong><br/>
        </div>
        <div class="actions">
          <%= relative_time(@issue.updated_on) %>
        </div>
      </div>
    
      <div class="issue-content">
        <% attachments = @issue.attachments - @issue.journals.map(&:own_attachments).flatten %>
        <% if @issue.description? || attachments.any? -%>
          <% if @issue.description? %>
            <div class="wiki description">
              <%= textilizable @issue, :description, :attachments => attachments %>
            </div>
          <% end %>
          <%= render 'attachments/links', { :attachments => attachments, :options => { deletable: false, author: false }, :thumbnails => true } %>
        <% end -%>
      </div>
    </div>
  </div>

  <ul>
    <li class="sub-menu-item" ><span>讨论此课题</span></li>
  </ul>

  <% if @journals.present? %>
    <div class="history">
      <%= render :partial => 'history', :locals => { :issue => @issue, :journals => @journals } %>
    </div>
  <% end %>

  <% if !@issue.closed? && User.current.allowed_to?(:add_issue_notes, @issue.project) %>
    <%= labelled_form_for @issue, html: { :multipart => true, :class => form_class, :id => form_class } do |f| %>
      <div class="row-fluid">
        <%= text_area_tag 'notes', @notes, :rows => 3, :class => 'wiki-edit span24', :placeholder => '在此添加讨论内容......' %>
        <% if old_browser?(@user_agent) %>
          <%= render :partial => 'attachments/form', :locals => {:container => @issue} %>
        <% end %>

        <%= submit_tag l(:button_submit), disable_with: l(:button_submiting), class: 'button blue full pull-right', id: "button_submit" %>
      </div>
    <% end -%>
  <% end -%>
  
</div>

<% html_title "#{@issue.tracker.name} ##{@issue.id}: #{@issue.subject}" %>
