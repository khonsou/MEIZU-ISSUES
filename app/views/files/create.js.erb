<% if @unsaved_attachments.present? %>
  $('.alert.alert-warning').text("<%= j @unsaved_attachments.first.errors.full_messages.join() %>").show();
<% else %>  
  $('.alert.alert-warning').hide();
<% end %>  
$('#files_list').replaceWith("<%= j render('list', { attachments: @attachments }) %>");