if ($('#container-modal').length > 0) {
  $('#container-modal').modal('hide');
  $('#container-modal').remove();
}

$("body").append("<%= j(render('account')) %>");
$('#container-modal').modal('show');
